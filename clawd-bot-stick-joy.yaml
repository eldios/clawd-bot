# Local development entry point for the M5StickC Plus + Joystick Hat.

substitutions:
  claude_token: !secret claude_token

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password
  ap:
    password: !secret ap_password

packages:
  board: !include boards/m5stickc-plus-joy.yaml
